DENTAL_MAX_IMAGE_SIZE_MB=8.0

# DENTAL_VUONG_DIR=                       # mặc định tự suy ra ../vuong, chỉ set nếu khác vị trí
DENTAL_DETECTOR_CONF_THRESHOLD=0.35
DENTAL_CLASSIFIER_CONF_THRESHOLD=0.5
DENTAL_INFERENCE_DEVICE=cpu                   # "cuda" nếu có GPU NVIDIA + đã cài torch bản GPU

DENTAL_OPENAI_API_KEY=
DENTAL_OPENAI_MODEL=gpt-4o-mini
